<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/scrollViewTurnos"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="#FAFAFA">

    <LinearLayout
        android:id="@+id/layoutTurnos"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- El TextView de la fecha se agrega dinámicamente desde Java -->
        <!-- El botón de cambio de vista también se agrega desde Java -->

        <!-- Aquí se añadirán dinámicamente los CardView con los turnos -->

    </LinearLayout>

</ScrollView>
