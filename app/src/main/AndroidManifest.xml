<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--Permiso para poder conectarse a Internet-->
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"

        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GymApp"

        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">
        <!--El network config es necesario para que funcione bien la conexion-->


        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="true">

        </activity>

        <activity
            android:name=".WelcomeActivity"
            android:exported="true">

        </activity>

        <activity
            android:name=".RegisterActivity"
            android:exported="true">

        </activity>

        <activity
            android:name=".ClaseActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".ProductoActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".ReservarClasesActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".CrearClaseActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".PerfilActivity"
            android:exported="true">
        </activity>




    </application>


</manifest>